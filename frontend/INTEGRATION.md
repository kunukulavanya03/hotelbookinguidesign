# Frontend ↔ Backend integration
- API base is taken from VITE_API_BASE_URL (defaults to http://localhost:8000)
- Use callEndpoint('<name>', payload) or apiRequest('<METHOD>', '<path>', data)
- See src/services/apiClient.ts for generated endpoints
